# crates/context-engine

包名 `novel-context-engine`。给续写拼上下文包（`context.assemble`）。

- **不是**编辑器上方预选条。预选条在 `novel-context-hints`。
- `assembly.rs` / `acp.rs` / `search.rs`。
- 宿主 command：`build_context_package`（UI 暂时没调，不要删 IPC）。
- 测试：`tests/acp_test.rs`。
