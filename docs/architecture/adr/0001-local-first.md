# ADR 0001: 本地优先架构

## 状态
已接受

## 背景
网文作家的正文是最核心的资产。云端不可用、模型失败或订阅过期不应导致无法写作。

## 决策
- 正文、版本、任务队列、记忆默认保存在本机 SQLite。
- 云同步、账户、订阅放在本地 MVP 之后。
- 模型调用支持用户自备 API Key 与本地模型。

## 后果
- 用户离线可用全部核心写作能力。
- 多设备同步与冲突解决推迟到后续版本。
- 数据迁移和备份必须在第一版可靠。
